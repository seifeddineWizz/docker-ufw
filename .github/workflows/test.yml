name: PR Verification

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]

jobs:
  test:
    name: Simple Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Run simple test
        run: |
          echo "PR verification started..."
          echo "Checking files..."
          ls -la
          echo "Test passed!"